export const Buttonlg = ({color, size, text, func}) => {
    return (
        <div className="bg-primary text-white py-5 px-16 rounded-full">
            <h1 className='text-3xl font-semibold'>{text}</h1>
        </div>
    )
}

export default Buttonlg;